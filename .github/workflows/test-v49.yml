name: Validate V49 branch

on:
  push:
    branches: [v49-cloud-probe-progress]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: windows-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install Inno Setup
        shell: pwsh
        run: choco install innosetup --no-progress -y

      - name: Build and validate complete V49 installer
        shell: pwsh
        env:
          APP_VERSION: 0.1.49.0
        run: .\build.ps1

      - name: Confirm installer outputs
        shell: pwsh
        run: |
          # Segundo push intencional: el workflow ya existe en la rama.
          if (-not (Test-Path "installer_output\Aspiradora-Xiaomi-Setup.exe")) { throw "Falta instalador V49" }
          if (-not (Test-Path "installer_output\Aspiradora-Xiaomi-Setup.exe.sha256")) { throw "Falta SHA-256 V49" }
          Get-Item "installer_output\Aspiradora-Xiaomi-Setup.exe"
          Get-Content "installer_output\Aspiradora-Xiaomi-Setup.exe.sha256"
